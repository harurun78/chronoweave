# Chronoweave environment variables (Vite, build-time)
# Copy to .env.local for local override; do NOT commit secrets.

# Public base path for static deploy. Defaults to "/" for local dev.
# GitHub Pages deploy uses "/chronoweave/" (set in .github/workflows/deploy.yml).
VITE_BASE_PATH=/
